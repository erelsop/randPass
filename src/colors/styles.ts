/**
 * Defines the color styles used in the application.
 *
 * The `prompt` property is the color used for prompts.
 * The `success` property is the color used for success messages.
 * The `error` property is the color used for error messages.
 * The `option` property is the color used for options.
 */
const styles = {
  prompt: "#B8ACAC",
  success: "#A8C2B6",
  error: "#5A4852",
  option: "#71A89E",
};

export default styles;